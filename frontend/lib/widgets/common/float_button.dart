import 'package:flutter/material.dart';

class FloattingButton extends StatelessWidget {
  const FloattingButton({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}